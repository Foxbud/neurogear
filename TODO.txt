TODO List:

IMMEDIATE:

Rework Scale classes to support multi-channeled data.



SHORT TERM:

Find a solution to LogisticActivation rounding error.
   -May have to change notation in related classes.

Use standard exception conventions to clean up all exceptions.
   -Add exceptions to Layer class (correct weight generation issue).

Update all javadoc to be more informative to users.

Add a sequence generating tool somewhere in utilities.

Create a very clean and robust way of saving and loading DataSets.
   -Maybe use a pre-existing spreadsheet library.



LONG TERM:

Begin work on a Network class.

Devise a way to effectively mutlithread network training.

Add a utility for automated hyperparameter searching.
   -Do research; find the most practical method (balance between efficiency and simplicity).